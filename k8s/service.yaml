apiVersion: v1
kind: Service
metadata:
  name: ferma-test-service
  labels:
    app: ferma-test
spec:
  selector:
    app: ferma-test
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
    name: metrics